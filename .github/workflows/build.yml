name: Build PIXEL OS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PIXEL-OS
        uses: actions/checkout@v4

      - name: Install image build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            live-build \
            debootstrap \
            xorriso \
            squashfs-tools \
            mtools \
            grub-pc-bin \
            grub-efi-amd64-bin \
            grub-efi-amd64-signed \
            shim-signed
          lb --version

          # Ubuntu's packaged live-build currently contains an old Contents
          # index URL. Debian now publishes the architecture Contents index
          # under the archive component (main/Contents-amd64.gz).
          sudo python3 - <<'PY'
          from pathlib import Path

          path = Path('/usr/lib/live/build/lb_chroot_linux-image')
          text = path.read_text()
          replacements = {
              '${LB_PARENT_MIRROR_CHROOT}/dists/${LB_PARENT_DISTRIBUTION}/Contents-${LB_ARCHITECTURES}.gz': '${LB_PARENT_MIRROR_CHROOT}/dists/${LB_PARENT_DISTRIBUTION}/main/Contents-${LB_ARCHITECTURES}.gz',
              '${LB_MIRROR_CHROOT}/dists/${LB_DISTRIBUTION}/Contents-${LB_ARCHITECTURES}.gz': '${LB_MIRROR_CHROOT}/dists/${LB_DISTRIBUTION}/main/Contents-${LB_ARCHITECTURES}.gz',
          }
          for old, new in replacements.items():
              text = text.replace(old, new)
          path.write_text(text)
          PY

      - name: Configure Debian live image
        run: |
          lb clean --purge || true
          lb config \
            --mode debian \
            --distribution bookworm \
            --architectures amd64 \
            --archive-areas "main contrib non-free-firmware" \
            --mirror-bootstrap https://deb.debian.org/debian \
            --mirror-chroot https://deb.debian.org/debian \
            --mirror-binary https://deb.debian.org/debian \
            --security true \
            --mirror-binary-security https://deb.debian.org/debian-security \
            --mirror-chroot-security https://deb.debian.org/debian-security \
            --binary-images iso-hybrid \
            --debian-installer false \
            --bootappend-live "boot=live components" \
            --apt-recommends true

      - name: Configure PIXEL OS packages
        run: |
          mkdir -p config/package-lists
          cat > config/package-lists/pixel.list.chroot <<'EOF'
          linux-image-amd64
          systemd
          dbus
          xorg
          xserver-xorg-video-all
          lightdm
          openbox
          nodejs
          npm
          git
          curl
          ca-certificates
          fonts-dejavu
          EOF

      - name: Install PIXEL shell into image
        run: |
          mkdir -p config/includes.chroot/opt/pixel-shell
          cp -a shell/. config/includes.chroot/opt/pixel-shell/
          mkdir -p config/includes.chroot/etc/lightdm
          cat > config/includes.chroot/etc/lightdm/lightdm.conf <<'EOF'
          [Seat:*]
          autologin-user=pixel
          autologin-session=pixel
          user-session=pixel
          EOF
          mkdir -p config/includes.chroot/usr/share/xsessions
          cat > config/includes.chroot/usr/share/xsessions/pixel.desktop <<'EOF'
          [Desktop Entry]
          Name=PIXEL OS
          Comment=PIXEL OS Electron desktop
          Exec=/usr/local/bin/pixel-session
          Type=Application
          EOF
          mkdir -p config/includes.chroot/usr/local/bin
          cat > config/includes.chroot/usr/local/bin/pixel-session <<'EOF'
          #!/bin/sh
          openbox-session &
          sleep 1
          exec /opt/pixel-shell/node_modules/electron/dist/electron /opt/pixel-shell --no-sandbox --disable-gpu-sandbox
          EOF
          chmod +x config/includes.chroot/usr/local/bin/pixel-session

      - name: Configure image setup
        run: |
          mkdir -p config/hooks/live
          cat > config/hooks/live/0100-pixel.hook.chroot <<'EOF'
          #!/bin/sh
          set -e
          useradd -m -s /bin/bash pixel || true
          usermod -aG audio,video,input pixel || true
          chown -R pixel:pixel /opt/pixel-shell
          cd /opt/pixel-shell
          npm install --no-audit --no-fund
          systemctl enable lightdm.service
          systemctl set-default graphical.target
          EOF
          chmod +x config/hooks/live/0100-pixel.hook.chroot

      - name: Build bootable USB image
        run: |
          sudo lb build
          mkdir -p artifact
          cp live-image-amd64.hybrid.iso artifact/PIXEL-OS-USB.iso
          sha256sum artifact/PIXEL-OS-USB.iso > artifact/PIXEL-OS-USB.iso.sha256
          file artifact/PIXEL-OS-USB.iso
          xorriso -indev artifact/PIXEL-OS-USB.iso -report_el_torito plain -report_system_area plain

      - name: Upload PIXEL OS USB image
        uses: actions/upload-artifact@v4
        with:
          name: PIXEL-OS-USB
          path: artifact/
